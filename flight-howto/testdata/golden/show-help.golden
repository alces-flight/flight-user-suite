NAME:
   flight howto show - Open a user guide for viewing in the terminal

USAGE:
   flight howto show [options] <index>

OPTIONS:
   --format FORMAT  use specified FORMAT for the output (pretty, json). (default: "pretty")
   --help, -h       show help
